import { ToastOptions } from 'react-hot-toast';

export const defaultSuccessToastOptions: ToastOptions = {
  className:
    'bg-secondary-50 border border-secondary-600/20 shadow-none text-xs text-secondary-600',
};

export const defaultErrorToastOptions: ToastOptions = {
  className:
    'bg-secondary-50 border border-secondary-600/20 shadow-none text-xs text-secondary-600',
};
